name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  checks:
    runs-on: nixos
    steps:
      - uses: actions/checkout@v5
      - name: Build flake checks
        run: nix-fast-build --flake .#checks
